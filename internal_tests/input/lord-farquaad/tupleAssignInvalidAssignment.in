 procedure main() returns integer {
     integer a = 5;
     tuple(real, integer) t = (5.0, 2);
     tuple(real, integer) q = (6.0, 3);
     t = (6.0, 3, 2);
     a -> std_output;
     '\n' -> std_output;
     return 0;
 }