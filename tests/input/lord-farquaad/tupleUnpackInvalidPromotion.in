 procedure main() returns integer {
     integer a = 5;
     integer b = 1;
     integer c = 0;
     tuple(real, integer) t = (5.0, 2);
     tuple(real, integer) q = (6.0, 3);
     b, a = t;
     a -> std_output;
     '\n' -> std_output;
     return 0;
 }